import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Practica_2_presupuesto',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
